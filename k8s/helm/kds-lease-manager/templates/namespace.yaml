apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    name: {{ .Values.namespace }}
    app.kubernetes.io/name: {{ include "kds-lease-manager.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}

